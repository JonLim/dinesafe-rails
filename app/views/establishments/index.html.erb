<div class="container">
  <div class="row">
    <h1>List of Establishments</h1>
  </div>

    <table class="table">
      <thead>
        <tr>
          <th>Name</th>
          <th>Address</th>
          <th></th>
        </tr>
      </thead>

      <tbody>
        <% @establishments.each do | establishment | %>
          <tr>
            <td><%= establishment.name %></td>
            <td><%= establishment.address %></td>
            <td><%= link_to 'Show', establishment %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <p><%= paginate @establishments %></p>

</div>

